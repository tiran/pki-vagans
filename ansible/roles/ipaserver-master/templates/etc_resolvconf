search {{ ansible_domain }}
{% for host in groups['ipaserver_master'] %}
# master {{ host }}
nameserver {{ hostvars[host]['ansible_default_ipv4']['address'] }}
{% endfor %}
{% for host in groups['ipaserver_replica'] %}
# replica {{ host }}
nameserver {{ hostvars[host]['ansible_default_ipv4']['address'] }}
{% endfor %}

# original forwarders
{% for address in dns_forwarder.nameservers %}
# nameserver {{ address }}
{% endfor %}
