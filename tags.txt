
playbook: ansible/playbook.yml

  play #1 (install Dogtag PKI CA):	TAGS: []
    TASK TAGS: [389ds, bootstrap, common, custom-rpms, dummy, firewall, ipa-install, lxc, network, package-copr, package-install, package-upgrade, pki, pki-install, pkica, pkikra]

  play #2 (install FreeIPA server master):	TAGS: []
    TASK TAGS: [bootstrap, common, custom-rpms, dummy, fetch, firewall, ipa, ipa-install, ipaserver, ipaserver-master, lxc, network, package-copr, package-install, package-upgrade, pki]

  play #3 (install FreeIPA server replica):	TAGS: []
    TASK TAGS: [bootstrap, common, custom-rpms, dummy, fetch, firewall, ipa, ipa-client, ipa-install, ipaserver, ipaserver-replica, lxc, network, package-copr, package-install, package-upgrade, pki, sleep]

  play #4 (install FreeIPA client):	TAGS: []
    TASK TAGS: [bootstrap, common, custom-rpms, dummy, firewall, ipa, ipa-client, ipa-install, lxc, network, package-copr, package-install, package-upgrade, pki, sleep]

